commands:
  rails: &rails_development
    prepend: "bundle exec"
    directory: "/vagrant"
    image: "ruby"
    volumes:
      - "/vagrant"
  rake:
    <<: *rails_development
  cucumber:
    <<: *rails_development
  guard:
    <<: *rails_development
  rspec:
    <<: *rails_development
