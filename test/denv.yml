commands:
  rails: &rails_development
    prepend: "bundle exec"
    directory: "/vagrant"
    image: "ruby"
    args: "--rm -v /vagrant:/vagrant"
  rake:
    <<: *rails_development
  cucumber:
    <<: *rails_development
  guard:
    <<: *rails_development
  rspec:
    <<: *rails_development
